-- DAY - ONE -- 28-26

SHOW DATABASES;

USE HR;
SHOW TABLES;

SELECT *
FROM EMPLOYEES;

SELECT SALARY
FROM EMPLOYEES;

-- OPERATORS --

-- ARITHMETIC
-- COMPARISON
-- LOGICAL
-- MEMBERSHIP
-- LIKE, BETWEEN, NULL

-- ARITHMETIC
-- +,-,*,/

SELECT SALARY, SALARY+1500 AS SALARY_INCREAMENT
FROM EMPLOYEES;

-- COMPARISON
-- <, >, <=, =>, =, <>
SELECT *
FROM EMPLOYEES
WHERE SALARY<10000;

select *
from employees
where salary<=10000;

SELECT * 
FROM EMPLOYEES
WHERE first_name="Alexander";

SELECT FIRST_NAME
FROM EMPLOYEES;

UPDATE EMPLOYEES
SET FIRST_NAME = "REX"
WHERE FIRST_NAME = "LEX";

-- SELECT COUNT(*) AS DISTCONT
-- FROM (SELECT DISTINCT SALARY FROM EMPLOYEES);

SELECT COUNT(DISTINCT SALARY)
FROM EMPLOYEES;

SELECT DISTINCT SALARY
FROM EMPLOYEES;

